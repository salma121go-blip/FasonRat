# FasonRat Environment Configuration
# Copy this file to .env and adjust values as needed.
# All variables are optional — the server runs with safe defaults without a .env file.

# Application
NODE_ENV=production

# Security
JWT_SECRET=123

# Logging
# level: "fatal", "error", "warn", "info", "debug", "trace"
LOG_LEVEL=info
